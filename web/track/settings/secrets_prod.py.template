
ALLOWED_HOSTS = [
	...,
]

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': '(db-name)',
        'USER': '(db-user)',
        'PASSWORD': '(db-password)',
        'HOST': '(db-host)',
        'PORT': 5432,
    }
}

